/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/hunt-properties-website',
  assetPrefix: '/hunt-properties-website',
  images: {
    unoptimized: true,
  },
}

export default nextConfig
